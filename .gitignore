node_modules
.next
out
build
dist

.env
.env.local
.env*.local

*.tsbuildinfo
next-env.d.ts

.DS_Store
.vercel
npm-debug.log*
yarn-error.log*
.claude/
.env*

# Keep the template — it contains no secrets.
!.env.example

# Prisma generated client (regenerated by postinstall)
src/generated

# Demo video — 28 MB, submitted as a separate file rather than bloating the repo.
# Rebuild with: npx tsx docs/capture_demo.mts && python3 docs/build_demo_video.py
docs/PathFinder-Demo.mp4
